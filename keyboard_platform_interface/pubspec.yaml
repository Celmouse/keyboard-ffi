name: keyboard_platform_interface
description: "A new Flutter plugin project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.4
  flutter: '>=3.3.0'
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

  keyboard_protocol:
    path: ../../../packages/protocol/keyboard_protocol

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
